// Alias commands to their modules.
// i.e. call `!eth` for the `ether.js` module

module.exports = {
  aliases: {
    'eth': 'ether',
    'e': 'ether',
    'g': 'google',
    'w': 'weather',
    'r': 'reload'
  }
}
